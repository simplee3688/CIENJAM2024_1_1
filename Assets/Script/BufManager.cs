using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BufManager
{
    List<Buf> bufList = new List<Buf>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void updeatBuf(Time time)
    {

    }
}
